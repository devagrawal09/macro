# `@macro/plugin-cli`

Local Macro Plugin check/build CLI. It does not publish, install, or run Plugins.

```sh
macro plugin check plugin.tsx
macro plugin build plugin.tsx --out-dir .macro-plugin/release
```

See [`docs/plugin-authoring.md`](../../docs/plugin-authoring.md) for the source contract and workflow.
